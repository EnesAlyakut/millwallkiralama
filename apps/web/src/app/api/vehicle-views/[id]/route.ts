import {db} from '@/lib/db';import {cookies} from 'next/headers';export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const ua=req.headers.get('user-agent')||'';if(/bot|crawler|spider|slurp/i.test(ua))return Response.json({ok:true});const {id}=await params;const jar=await cookies();const key=`view_${id.slice(0,16)}`;if(jar.has(key))return Response.json({ok:true});await db.vehicle.updateMany({where:{id,deletedAt:null},data:{viewCount:{increment:1}}});jar.set(key,'1',{httpOnly:true,sameSite:'lax',maxAge:86400,path:'/'});return Response.json({ok:true})}
